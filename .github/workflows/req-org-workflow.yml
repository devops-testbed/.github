name: RequiredChecks
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
  schedule: 
    - cron: "*/5 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    permissions: write-all
    steps:
      - name: Run Required Check
        run: echo Hello from required org action
      - name: List Runners
        run : gh api /repos/${{github.repository}}/actions/runners
